import "./PreviewCV.scss";
import type { FormData } from "./BuilderCV";

interface PreviewCVProps {
  formData: FormData;
}

const PreviewCV = (_props: PreviewCVProps) => {
  return (
    <div className="preview-cv">
      {/*Це патом*/}
    </div>
  );
};

export default PreviewCV;
